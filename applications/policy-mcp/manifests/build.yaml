apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: policy-mcp
  namespace: apollo-dev
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: policy-mcp
  namespace: apollo-dev
spec:
  source:
    type: Binary
    binary: {}
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Containerfile
  output:
    to:
      kind: ImageStreamTag
      name: policy-mcp:latest
  triggers: []